extends ItemType
class_name Trap

var fishing_power: float = 0.0 # Fishing power that the trap will have.
var fishing_speed: float = 0.0 # How fast the trap will catch stuff.
var space: int = 5 # How much space the trap will have.
var bait_storage: int = 10 # How much bait the trap can store
var data: Dictionary = {}
var submerged_texture: Texture2D # Texture the trap makes when submerged.
